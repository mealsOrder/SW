/**
 * Naming Conventions
 * 
 * 변수 이름 지을떄
 * 1) 일반적으로 영어 문자 숫자
 * 2) 특수기호는 _ $ 사용할수 있다
 * 3) 숫자로 이름을 시작 할 수 없다.
 * 4) 키워드는 변수명으로 사용 할 수 없다.
 *      var const = 'var';
 */

let a='a';
let ho = _titi;

/**
 * Naming Convention 2
 * 
 * 1) camelCase - 대부분의 언어에서도 많이 사용
 * 2) snake_case -> 파이썬
 * 3) PascalCase -> C# 마이크로소프트 게임이 언어에서 사용
 */
