#include<iostream>
#include<vector>
#include<algorithm>
#include<queue>
using namespace std;
int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int N;
    cin >> N;
    vector<int>v(N);
    vector<int>tar(N);
    
    for(int i=0;i<N;i++)cin >> v[i];
    tar = v;
    sort(v.begin(),v.end());

    for(int )
    return  0;
}