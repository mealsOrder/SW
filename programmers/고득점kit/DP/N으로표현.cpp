#include <string>
#include <vector>
#include <set>
using namespace std;
int solution(int N, int num) {
    
    string stn = ;
    fill(dp,dp+MX,8);
    for(int i=0;i<MX;i++){
        ans = min(ans,dp[i]);
    }

    if(ans > 8) return -1;
    else return ans;
}